# Koremiyo (次、コレ見よ) AI Agent Rules & Project Specs

## プロジェクト基本ルール
1. **回答言語**: すべて日本語で作成すること。
2. **AI間共通仕様書の参照**: プロジェクトの最新仕様・コンポーネント構造・カラーパレット・DB設計・API仕様は `AI_SHARED_CONTEXT.md` および `PROJECT_SPEC.md` に集約されています。開発セッション開始時に必ず `AI_SHARED_CONTEXT.md` を最優先でロードしてください。
3. **仕様変更時の追記義務 (MANDATORY)**: 機能追加、テーブル・カラム変更、API追加、デザイン・認証変更等の仕様変更を行ったAIセッションは、必ず作業完了時に `AI_SHARED_CONTEXT.md` および `PROJECT_SPEC.md` に最新仕様と変更履歴を追記してください。
4. **デザインルール**:
   - 背景色: 目に優しいソフトスレートグレー (`--bg-color: #edf2f7`)
   - 文字色: 高コントラストの濃いチャコールブラック (`--text-primary: #0f172a`, `--text-secondary: #334155`)
   - アクセント: エメラルドグリーン (`--primary-color: #059669`)
5. **セキュリティルール**:
   - 管理者ボタンはデフォルトで一般非表示。解錠用URL `?admin_key=koremiyo2026` 経由でのみ表示される仕様を保持すること。
6. **検証義務**:
   - タスク完了前には必ず `npm run build` を実行し、ビルドエラーがないことを検証すること。
