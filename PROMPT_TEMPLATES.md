# AI並列開発用 プロンプトテンプレート集 (`PROMPT_TEMPLATES.md`)

本ファイルは、「次、コレ見よ」(koremiyo-app) の開発を**複数のAIチャット（または複数のAIエージェント）で並列して進める際に使用するプロンプトテンプレート集**です。

---

## 📋 使い方

1. **新しいチャットを立ち上げた時** 👉 **【テンプレート1】** をコピーしてAIに送信します。
2. **新機能を開発・依頼する時** 👉 **【テンプレート3】** で具体要件をAIに提示します。
3. **機能開発・修正が完了した時** 👉 **【テンプレート2】** を送信して、AIに `PROJECT_SPEC.md` を最新状態に更新させます。

---

## 🚀 【テンプレート1】新規チャット初回起動用プロンプト (最優先)

新しいAIチャット（スレッド/エージェント）を開いた際、最初に以下のプロンプトをそのままコピーして送信してください。

```markdown
あなたは本プロジェクト「次、コレ見よ」(koremiyo-app) の開発を担当するAIエンジニアです。
複数のAIチャット間で開発仕様の完全な共通認識を保つため、まず `AI_SHARED_CONTEXT.md`（AI間共有コンテキスト仕様書）を最優先でロードし、以下の仕様を把握してください。

1. システム全体構成 (Next.js 16.2.4 App Router / Supabase / Vercel / 本番: https://koremiyo-anime.online)
2. デザインシステム規則 (`src/app/globals.css` のCSS変数: `--bg-color: #edf2f7`, `--text-primary: #0f172a`, `--primary-color: #059669`、二重枠禁止)
3. セキュリティ & 管理者仕様 (一般画面では非表示、`?admin_key=koremiyo2026` による解錠プロトコル)
4. データベース設計 (posts, ranked_products, campaigns, feedbacks)
5. 最新API & 自動化タスク (/api/dlsite/ranking, /api/cron/scheduled-weekly-poster 等)

【重要遵守事項】
- 回答およびコードコメントはすべて「日本語」で作成してください。
- 作業完了前には必ず `npm run build` で静的検証を行ってください。
- 【最重要】本チャットで新機能開発・API・DB変更・UI改修など仕様変更を行った場合は、完了時に必ず `AI_SHARED_CONTEXT.md` に最新仕様と変更履歴を追記・更新してください。

内容を把握したら、「AI_SHARED_CONTEXT.md をロードし、最新仕様と開発ルール（仕様変更時の追記義務を含む）を把握しました。どのような作業を行いますか？」と返答してください。
```

---

## 📝 【テンプレート2】機能開発完了時の設計書更新用プロンプト

機能の実装や修正が完了し、テスト・動作確認が取れたら、同じチャット内で以下のプロンプトを送信して `PROJECT_SPEC.md` を更新させてください。

```markdown
今回の機能開発・修正作業が完了しました。
複数チャットでの並列開発をスムーズに維持するため、今回実施した変更内容を `AI_SHARED_CONTEXT.md` (AI間共有コンテキスト仕様書) および `PROJECT_SPEC.md` に反映・更新してください。

【更新時の注意事項】
- `AI_SHARED_CONTEXT.md` の該当ブロック（routes, api_endpoints, database_schemas 等）を最新化してください。
- `AI_SHARED_CONTEXT.md` の末尾 `mutation_log` に、本日の日付・変更種別・実施内容の要約を追記してください。
- 既存の他の仕様や設定を誤って消去しないよう注意してください。
- 完了後、`npm run build` でビルドが通ることを最終確認してください。
```

---

## 🛠️ 【テンプレート3】機能開発・修正リクエスト用プロンプト

具体的な新機能の開発やバグ修正を依頼する際は、以下のフォーマットで指示を出すとAIが誤解なく正確に実装できます。

```markdown
【機能開発リクエスト】

■ 開発目標 / 機能名:
[例: ユーザーのお気に入り作品保存機能の追加]

■ 概要・達成したいこと:
[例: ブラウザの localStorage または Supabase を使用して、ユーザーが気になる作品を保存・一覧表示できるようにしたい]

■ 要求仕様:
- [要件1: 保存ボタンを押すと状態が切り替わる]
- [要件2: 既存のデザインシステム `globals.css` のクラス・CSS変数に準拠する]
- [要件3: 二重枠などの不格好な枠線は作らない]

■ 開発手順:
1. まず必要となるファイル・変更点をリストアップしてください。
2. コードの作成・修正を行い、ビルドエラーが発生しないか検証してください。
3. 完了後、`PROJECT_SPEC.md` の更新準備を行ってください。
```

---

## 💡 並列開発におけるAI運用のコツ

- **CSSのブレを防ぐ**: 必ず `src/app/globals.css` で定義された `--primary-color` や `.glass-panel` 等を使用させることで、チャットが別々でも統一感のあるUIが保たれます。
- **仕様の先祖返りを防ぐ**: 各チャットでの作業完了時に【テンプレート2】を実行させて `PROJECT_SPEC.md` を最新に保つことで、別のチャットが古い仕様でコードを書き換える「先祖返り」を防げます。
